# Razorpay test-mode credentials — get these free from https://dashboard.razorpay.com/app/keys
RAZORPAY_KEY_ID=rzp_test_TYFKPHd9MP5X1o

RAZORPAY_KEY_SECRET=paZyzvIoEXhqH0nG14SJemSZ


# Webhook secret — set the SAME value here and in the Razorpay Dashboard
# (Settings > Webhooks > Add New Webhook > Secret)
RAZORPAY_WEBHOOK_SECRET=your_webhook_secret_here

# Server port
PORT=3000

# Optional: if set, PayGenie uses an LLM to parse commands instead of the
# built-in regex parser. Works with any Anthropic-compatible key.
ANTHROPIC_API_KEY=
